export class Quote {
  // public title: string;
  public quote: string;
  public author: string;

  constructor(quote: string, author: string) {
    // this.title = title;
    this.quote = quote;
    this.author = author;
  }
}
